{
  "name": "gamehorde",
  "version": "1.0.0",
  "description": "GameHorde - Video Game Collection Management",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm ci --legacy-peer-deps && npm run build",
    "install": "cd backend && npm ci && cd ../frontend && npm ci --legacy-peer-deps",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
